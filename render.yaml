# One-click backend deploy on Render (https://render.com).
# In Render: New + → "Blueprint" → connect this repo → it reads this file.
services:
  - type: web
    name: final-whistle-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free          # free spins down on idle (cold start ~30s); upgrade to keep warm
    healthCheckPath: /api/config
    autoDeploy: true
    envVars:
      - key: TXLINE_MODE
        value: live                  # real mainnet TxLINE World Cup feed
      - key: TXLINE_BASE_URL
        value: https://txline.txodds.com   # mainnet TxLINE host
      - key: TXLINE_COMPETITION_ID
        value: "72"                  # FIFA World Cup competition id
      - key: NEXT_PUBLIC_SOLANA_CLUSTER
        value: mainnet-beta          # mainnet identity/anchor display
      - key: NEXT_PUBLIC_SOLANA_RPC
        value: https://api.mainnet-beta.solana.com
      # SECRET — Render prompts you to paste this during Blueprint setup.
      # Copy the value from your local .env.local (the TXLINE_API_TOKEN=... line).
      - key: TXLINE_API_TOKEN
        sync: false
      # Optional extras:
      # - key: SOLANA_ANCHOR_SECRET_KEY  (anchor room proofs on-chain)
      #   sync: false
      # - key: ANTHROPIC_API_KEY         (Claude-written recaps)
      #   sync: false
